import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:limpa_agenda_whatsapp/app/pages/agenda/agenda_page.dart';

main() {
  testWidgets('AgendaPage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(AgendaPage(title: 'Agenda')));
    //  final titleFinder = find.text('Agenda');
    //  expect(titleFinder, findsOneWidget);
  });
}
