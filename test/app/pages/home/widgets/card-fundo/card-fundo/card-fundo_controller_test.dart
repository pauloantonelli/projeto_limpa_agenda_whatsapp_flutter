import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:limpa_agenda_whatsapp/app/pages/home/home_module.dart';

void main() {
  initModule(HomeModule());
  // Card-fundoController card-fundo;
  //
  setUp(() {
    //     card-fundo = HomeModule.to.get<Card-fundoController>();
  });

  group('Card-fundoController Test', () {
    //   test("First Test", () {
    //     expect(card-fundo, isInstanceOf<Card-fundoController>());
    //   });

    //   test("Set Value", () {
    //     expect(card-fundo.value, equals(0));
    //     card-fundo.increment();
    //     expect(card-fundo.value, equals(1));
    //   });
  });
}
