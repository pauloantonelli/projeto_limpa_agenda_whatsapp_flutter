import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:limpa_agenda_whatsapp/app/shared/widgets/link-button/link-button/link-button_controller.dart';
import 'package:limpa_agenda_whatsapp/app/shared/shared_module.dart';

void main() {
  initModule(SharedModule());
  // Link-buttonController link-button;
  //
  setUp(() {
    //     link-button = SharedModule.to.get<Link-buttonController>();
  });

  group('Link-buttonController Test', () {
    //   test("First Test", () {
    //     expect(link-button, isInstanceOf<Link-buttonController>());
    //   });

    //   test("Set Value", () {
    //     expect(link-button.value, equals(0));
    //     link-button.increment();
    //     expect(link-button.value, equals(1));
    //   });
  });
}
